(function () {
    var AuthAddManager = {
        //初始化
        init: function () {
            var that = this;
            that.bindEvent();
        },
        bindEvent: function () {   
            
        }
    };
    $(function () {
        AuthAddManager.init();
    });
})(window);
